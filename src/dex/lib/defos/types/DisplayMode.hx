package dex.lib.defos.types;

typedef DisplayMode = {
	var width:Int;

	var height:Int;
	var scaling_factory:Int;
	var refresh_rate:Int;
	var bits_per_pixel:Int;
	var orientation:Int;
	var reflect_x:Bool;
	var reflect_y:Bool;
}
